export interface Transcript {
    studentId: string;
    semester: string;
    academicYear: string;
    courseCode: string;
    courseName: string;
    classCode: string;
    credits: number;
    grade: number | null;
}